# Build an End-to-End V1 Prototype

We will build the first version as a vertical slice from PDF ingestion through structured answer rendering, rather than building ingestion, retrieval, or UI in isolation. This gives us an early hands-on quality signal for the actual field-technician workflow, even if each extractor starts basic.
