# Use FastAPI and React Vite

The prototype will expose a strong FastAPI backend with explicit ingestion, retrieval, answering, source, and evaluation endpoints, plus a thin React/Vite frontend for the hands-on experience. This keeps retrieval and answer contracts centralized in the API while still validating citations and source previews through a real UI.
